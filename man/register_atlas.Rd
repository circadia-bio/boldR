% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register_atlas.R
\name{register_atlas}
\alias{register_atlas}
\title{Register a custom brain atlas}
\usage{
register_atlas(
  labels_img,
  metadata,
  name = "custom",
  space = "MNI152NLin2009cAsym"
)
}
\arguments{
\item{labels_img}{A \code{niftiImage} (3D integer) or a file path to a NIfTI
label map. Voxels containing \code{0} are treated as background.}

\item{metadata}{A data frame with at minimum two columns:
\describe{
\item{\code{label}}{Integer. The integer value in \code{labels_img}.}
\item{\code{name}}{Character. Human-readable ROI name.}
}
Additional columns (hemisphere, network, x/y/z centroid, colour, etc.)
are stored and carried through to output.}

\item{name}{Character. A short identifier for this atlas
(e.g. \code{"Schaefer200"}, \code{"AAL3"}, \code{"custom"}).}

\item{space}{Character. The standard space this atlas is defined in.
Default \code{"MNI152NLin2009cAsym"}. Must match the space used in
\code{\link[=read_fmriprep]{read_fmriprep()}} / \code{\link[=parcellate]{parcellate()}}.}
}
\value{
A list of class \code{boldR_atlas} with components:
\describe{
\item{labels_img}{\code{niftiImage}. The 3D integer label map.}
\item{metadata}{Data frame. ROI metadata.}
\item{name}{Character. Atlas identifier.}
\item{space}{Character. Standard space.}
\item{n_rois}{Integer. Number of unique non-zero labels.}
}
}
\description{
Validates and registers a NIfTI label map and optional metadata table as a
\code{boldR_atlas} object. The schema is intentionally minimal: any parcellation
that provides a 3D integer label image and a label-to-name mapping can be
registered and passed to \code{\link[=parcellate]{parcellate()}}. Bundled atlas helpers (Schaefer,
AAL, Glasser) use this same interface.
}
\examples{
\dontrun{
atlas <- register_atlas(
  labels_img = "atlases/Schaefer2018_200Parcels_7Networks_MNI.nii.gz",
  metadata   = read.csv("atlases/Schaefer2018_200Parcels_labels.csv"),
  name       = "Schaefer200",
  space      = "MNI152NLin2009cAsym"
)
atlas
}
}
