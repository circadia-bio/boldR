% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parcellate.R
\name{parcellate}
\alias{parcellate}
\title{Extract ROI-level timeseries from a BOLD image}
\usage{
parcellate(bold, atlas, summary_fn = mean, min_voxels = 10L)
}
\arguments{
\item{bold}{A \code{boldR_bold} object from \code{\link[=prepare_bold]{prepare_bold()}}.}

\item{atlas}{A \code{boldR_atlas} object from \code{\link[=load_atlas]{load_atlas()}}.}

\item{summary_fn}{Function. Applied across voxels within each ROI at each
timepoint. Default \code{mean}. Common alternatives: \code{median}.}

\item{min_voxels}{Integer. Minimum number of in-mask voxels an ROI must
contain to be included in the output. ROIs below this threshold are
returned with \code{NA} timeseries and a warning. Default \code{10L}.}
}
\value{
A list of class \code{boldR_parcellated} with components:
\describe{
\item{timeseries}{Numeric matrix, timepoints x ROIs. Column names are ROI
labels from the atlas.}
\item{roi_voxel_counts}{Integer vector. Number of in-mask voxels per ROI.}
\item{atlas}{The \code{boldR_atlas} object used.}
\item{bold}{The \code{boldR_bold} object used.}
\item{n_rois}{Integer. Number of ROIs in output.}
\item{n_timepoints}{Integer. Number of timepoints.}
}
}
\description{
Applies a parcellation atlas to a preprocessed BOLD image, returning
mean timeseries per ROI. Works with any \code{boldR_atlas} object -- built-in
or custom -- as long as the atlas is in the same template space as the BOLD.
}
\examples{
\dontrun{
fprep  <- read_fmriprep("data/derivatives/fmriprep", subject = "01")
bold   <- prepare_bold(fprep, tr = 2, drop_volumes = 4)
atlas  <- load_atlas("schaefer100_7n")
parcel <- parcellate(bold, atlas)
dim(parcel$timeseries)  # timepoints x 100
}
}
