% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_bold.R
\name{export_bold}
\alias{export_bold}
\title{Export parcellated BOLD data for syncR}
\usage{
export_bold(parcellated, fc = NULL, roi_metrics = NULL, participant_id = NULL)
}
\arguments{
\item{parcellated}{A \code{boldR_parcellated} object from \code{\link[=parcellate]{parcellate()}}.}

\item{fc}{A \code{boldR_fc} object from \code{\link[=compute_fc]{compute_fc()}}, or \code{NULL} (default).}

\item{roi_metrics}{A tibble from \code{\link[=compute_roi_metrics]{compute_roi_metrics()}}, or \code{NULL}
(default). If \code{NULL}, metrics are computed automatically.}

\item{participant_id}{Character or \code{NULL}. Override the subject label for
use in \code{syncR}. If \code{NULL}, taken from the \code{boldR_bold} object.}
}
\value{
A list of class \code{boldR_export} ready for \code{syncR::sync()}.
}
\description{
Packages the output of \code{\link[=parcellate]{parcellate()}} into a \code{boldR_export} object
suitable for ingestion by \code{syncR::sync()}. The export includes the ROI
timeseries matrix, functional connectivity matrix (if computed), ROI
metrics, and participant metadata.
}
\examples{
\dontrun{
parcel <- parcellate(bold, atlas)
fc     <- compute_fc(parcel)
exp    <- export_bold(parcel, fc = fc)

# Pass to syncR
syncR::sync(..., bold = exp)
}
}
