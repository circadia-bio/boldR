% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_fmriprep.R
\name{read_fmriprep}
\alias{read_fmriprep}
\title{Read fMRIPrep BIDS derivatives for a participant}
\usage{
read_fmriprep(
  derivatives_dir,
  subject,
  session = NULL,
  task = NULL,
  space = "MNI152NLin2009cAsym",
  resolution = NULL
)
}
\arguments{
\item{derivatives_dir}{Character. Path to the fMRIPrep derivatives root
(e.g. \code{"sub-01/func/"}... or the top-level \verb{derivatives/fmriprep/}).}

\item{subject}{Character. Subject label without the \verb{sub-} prefix
(e.g. \code{"01"}).}

\item{session}{Character or \code{NULL}. Session label without the \verb{ses-} prefix.
If \code{NULL} (default), assumes a single-session dataset.}

\item{task}{Character or \code{NULL}. Task label (e.g. \code{"rest"}). If \code{NULL},
the first task found is used.}

\item{space}{Character. Template space for the BOLD and mask files.
Default \code{"MNI152NLin2009cAsym"}.}

\item{resolution}{Character or \code{NULL}. Resolution label (e.g. \code{"2"}).
If \code{NULL}, resolution is not filtered.}
}
\value{
A list of class \code{boldR_fmriprep} with components:
\describe{
\item{bold}{Character. Path to the BOLD NIfTI file.}
\item{bold_mask}{Character. Path to the brain mask NIfTI file.}
\item{t1w}{Character. Path to the T1w NIfTI file in template space.}
\item{t2w}{Character or \code{NA}. Path to the T2w NIfTI file if available.}
\item{dseg}{Character. Path to the discrete segmentation NIfTI file.}
\item{confounds}{Character. Path to the confounds TSV file.}
\item{subject}{Character. Subject label.}
\item{session}{Character or \code{NA}. Session label.}
\item{task}{Character. Task label.}
\item{space}{Character. Template space.}
\item{derivatives_dir}{Character. Resolved path to the derivatives root.}
}
}
\description{
Reads preprocessed fMRI outputs from an
\href{https://fmriprep.org}{fMRIPrep} BIDS derivatives directory for a single
participant and session, returning a structured \code{boldR_fmriprep} object
containing paths to the BOLD, T1w, T2w, and segmentation files.
}
\examples{
\dontrun{
fprep <- read_fmriprep(
  derivatives_dir = "data/derivatives/fmriprep",
  subject         = "01",
  task            = "rest"
)
fprep
}
}
