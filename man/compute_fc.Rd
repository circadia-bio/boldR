% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_fc.R
\name{compute_fc}
\alias{compute_fc}
\title{Compute functional connectivity from parcellated BOLD timeseries}
\usage{
compute_fc(parcellated, method = c("pearson", "partial"), fisher_z = TRUE)
}
\arguments{
\item{parcellated}{A \code{boldr_parcellated} object from \code{\link[=parcellate]{parcellate()}}.}

\item{method}{Character. Connectivity metric. One of:
\describe{
\item{\code{"pearson"}}{(Default) Pearson product-moment correlation.}
\item{\code{"partial"}}{Partial correlation (requires \code{corpcor} in Suggests).}
}}

\item{fisher_z}{Logical. Whether to apply Fisher r-to-z transformation
before returning values. Default \code{TRUE}. Diagonal is set to \code{NA}.}
}
\value{
A list of class \code{boldr_fc} with components:
\describe{
\item{matrix}{Numeric matrix (n_rois × n_rois). Symmetric FC matrix with
\code{NA} on the diagonal.}
\item{long}{Data frame (long format). Columns: \code{roi_i}, \code{roi_j},
\code{roi_name_i}, \code{roi_name_j}, \code{fc}.}
\item{method}{Character. Connectivity method used.}
\item{fisher_z}{Logical. Whether Fisher z was applied.}
\item{atlas_name}{Character. Atlas identifier.}
}
}
\description{
Estimates a ROI-to-ROI functional connectivity matrix from a parcellated
BOLD timeseries using Pearson correlation (default) or partial correlation.
Returns both the full symmetric matrix and a tidy long-format data frame.
}
\examples{
\dontrun{
parc <- parcellate(cleaned, atlas)
fc   <- compute_fc(parc)
dim(fc$matrix)
head(fc$long)
}
}
